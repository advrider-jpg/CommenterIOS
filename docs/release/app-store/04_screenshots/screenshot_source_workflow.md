# Screenshot Source Workflow

1. Use the fake `Room 5` / `Ava Ng` data only.
2. Run the screenshot UI tests or manually capture the final app UI.
3. Compare final captures against the draft captions.
4. Replace draft masters only after confirming the UI matches the shipped build.
5. If iPad support is enabled later, add iPad screenshots.
